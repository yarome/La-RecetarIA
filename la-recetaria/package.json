{
  "name": "la-recetaria",
  "version": "0.1.0",
  "private": true,
  "description": "La RecetarIA — recipe catalogue, weekly planner, and shopping list",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n be,fe -c blue,magenta \"npm:dev -w backend\" \"npm:dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "seed:users": "npm run seed:users -w backend",
    "seed:sample-recipes": "npm run seed:sample-recipes -w backend",
    "typecheck": "npm run typecheck -w backend && npm run typecheck -w frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
